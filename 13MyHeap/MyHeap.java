import java.util.*;

public class MyHeap{
   	private int[] values;
    private int size;
    private heap
	private int isMax;
 MyHeap() -> creates a max heap
public MyHeap(boolean isMax) -> creates a max-heap when boolean is true, min-heap when the boolean is false

public String toString()

    public int peak(){
 
 }
public int remove() ->


 remove the root and return the value  O(logn)

public void add(int) -> add the int to the heap  O(logn)
     if isMax

public int peek() -> return the value of the root but do not remove it.  O(1)

 }
public int remove() ->


 remove the root and return the value  O(logn)

public void add(int) -> add the int to the heap  O(logn)
     if isMax

	 public int peek() {
// return the value of the root but do not remove it.  O(1)
							     return heap[0];
							     }